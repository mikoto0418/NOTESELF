<Window x:Class="NOTESELF.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="NOTESELF" Height="640" Width="960" MinHeight="520" MinWidth="760">
    <Grid>
        <Border
            Margin="24"
            Padding="28"
            CornerRadius="16"
            Background="#111827">
            <StackPanel>
                <TextBlock
                    FontSize="28"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="NOTESELF" />
                <TextBlock
                    Margin="0,12,0,0"
                    FontSize="16"
                    Foreground="#D1D5DB"
                    Text="WPF 重写版正在初始化。当前已完成数据层、状态兼容层和测试骨架。" />
                <TextBlock
                    Margin="0,24,0,0"
                    FontSize="14"
                    Foreground="#9CA3AF"
                    Text="下一阶段将补齐主界面、桌面便签窗口、托盘与编辑器。" />
            </StackPanel>
        </Border>
    </Grid>
</Window>

